# Frontend Environment Variables
# Copy this file to .env.local for local development

# API Configuration
# The backend API URL - should match RECURSIA_BACKEND_PORT environment variable
# Default: http://localhost:8080 (matches backend default)
VITE_API_URL=http://localhost:8080

# Build Mode
# Set to 'production' for production builds
# Vite automatically sets this, but you can override
# VITE_MODE=development